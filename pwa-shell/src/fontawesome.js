import { library } from '@fortawesome/fontawesome-svg-core';
import {
  faArrowLeft,
  faBars,
  faBell,
  faCheckDouble,
  faSearch,
  faUserCircle,
  faTimes,
  faExclamationCircle,
} from '@fortawesome/free-solid-svg-icons';

const icons = [
  faArrowLeft,
  faBars,
  faBell,
  faCheckDouble,
  faSearch,
  faUserCircle,
  faTimes,
  faExclamationCircle,
];

library.add(...icons);
